#ifndef COMPARE
    #define COMPARE
    //Compare take as arguments two paths to two files
    void Compare(char *path1, char *path2);
    void MergeFiles(char * path1,char * path2,char * Destination);
#endif